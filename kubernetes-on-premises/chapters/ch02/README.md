# Chapter 2 — Who Is Liable

**State this chapter expects:** Chapter 1 completed, so that
`evidence/ch01/dependency-inventory.csv` and `evidence/REGISTER.csv` exist.

`obligations.csv` is a **template with five example rows**, not an answer.
The obligations are a property of your organisation, not of your cluster.
Replace every row, and leave the artifact column blank wherever you cannot
name a file that already exists. The blanks are the work.

Lab 2A rewrites git history on purpose and then restores it. If you would
rather not do that in a clone you care about, run it in a throwaway copy.
