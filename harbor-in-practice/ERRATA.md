# Errata

Differences between the printed book and later releases of the software.
Each entry names the chapter, the step, and what to do instead.

Nothing yet — the book is not published.

## Format

**Chapter N, Step M** — since `<component> <version>`, `<what changed>`.
The step as printed reads `<old>`; use `<new>` instead.
