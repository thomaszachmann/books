# Chapter 03 — Initialize, Seal, Unseal

**Starting state:** Vault running, uninitialised, sealed

**What this chapter builds:** An initialised, unsealed Vault plus init.json

Run `make reset` from the repository root to return to an uninitialised
Vault if you need a clean start.
