# Chapter 02 — Building Your Lab

**Starting state:** Nothing. Docker installed.

**What this chapter builds:** TLS certificate, vault.hcl, docker-compose.yml, reset script

Run `make reset` from the repository root to return to an uninitialised
Vault if you need a clean start.
