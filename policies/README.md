# Policies

Every policy the book writes, named by the chapter that introduces it.

Apply one with:

```bash
vault policy write <name> policies/<file>.hcl
```

The deliberately broken ones are kept. Chapter 6 asks you to run them and
find out why they fail — that is the lab, not an oversight.
